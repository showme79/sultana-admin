export default (theme) => ({
  root: {},

  title: theme.title,

  actions: {
    padding: [[theme.spacing(2), theme.spacing(4)]],
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
